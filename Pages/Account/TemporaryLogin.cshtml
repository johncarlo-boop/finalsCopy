@page
@model PropertyInventory.Pages.Account.TemporaryLoginModel
@{
    ViewData["Title"] = "Login with Temporary Password";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Property Inventory</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    <style>
        body {
            background: linear-gradient(135deg, #006400 0%, #004d00 50%, #006400 100%);
            min-height: 100vh;
            margin: 0;
            padding: 0;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .login-card {
            max-width: 450px;
            width: 100%;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
            background: white;
            border-radius: 15px;
        }
        .btn-dark-green {
            background-color: #006400;
            border-color: #006400;
            color: white;
        }
        .btn-dark-green:hover {
            background-color: #004d00;
            border-color: #004d00;
            color: white;
        }
        .text-dark-green {
            color: #006400 !important;
        }
        .input-group-text {
            background-color: #006400;
            color: white;
            border-color: #006400;
        }
        .email-display {
            background-color: #f8f9fa;
            padding: 12px;
            border-radius: 5px;
            font-weight: 500;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="card login-card">
            <div class="card-body p-5">
                <div class="text-center mb-4">
                    <h2 class="mb-3">Welcome!</h2>
                    <p class="text-muted">Your account has been approved. Please enter your temporary password to continue.</p>
                </div>

                @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="bi bi-exclamation-circle"></i> @Model.ErrorMessage
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                @if (TempData["ErrorMessage"] != null)
                {
                    <div class="alert alert-danger alert-dismissible fade show" role="alert">
                        <i class="bi bi-exclamation-circle"></i> @TempData["ErrorMessage"]
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                }

                <form method="post">
                    <div asp-validation-summary="All" class="text-danger mb-3"></div>

                    <div class="mb-3">
                        <label class="form-label">Email Address</label>
                        <div class="email-display">
                            <i class="bi bi-envelope"></i> @Model.Email
                        </div>
                        <input type="hidden" asp-for="Email" />
                    </div>

                    <div class="mb-3">
                        <label asp-for="TemporaryPassword" class="form-label"></label>
                        <div class="input-group">
                            <span class="input-group-text"><i class="bi bi-lock"></i></span>
                            <input asp-for="TemporaryPassword" class="form-control" autocomplete="off" placeholder="Enter your temporary password" />
                        </div>
                        <span asp-validation-for="TemporaryPassword" class="text-danger"></span>
                        <small class="form-text text-muted">This is the temporary password sent to your email.</small>
                    </div>

                    <div class="d-grid">
                        <button type="submit" class="btn btn-dark-green btn-lg">
                            <i class="bi bi-box-arrow-in-right"></i> Continue to Dashboard
                        </button>
                    </div>
                </form>

                <div class="mt-4 text-center">
                    <p class="mb-2">
                        <a asp-page="./Login" class="text-dark-green text-decoration-none">
                            <i class="bi bi-arrow-left"></i> Back to Login
                        </a>
                    </p>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <partial name="_ValidationScriptsPartial" />
</body>
</html>

